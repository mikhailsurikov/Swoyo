# Домашнее задание. Стандартная библиотека Python

## Задание 1. Упаковка товаров

На складе товары складывают в коробки одинаковой вместимости.

Напишите функцию:

```python
boxes_needed(items, box_size)
```

Она принимает:

* количество товаров;
* вместимость одной коробки.

Используйте `math.ceil()`, чтобы определить минимальное необходимое количество коробок.

### Пример

```python
print(boxes_needed(137, 12))
```

### Результат

```text
12
```

---

## Задание 2. Дата доставки

Дана дата отправки заказа:

```python
order_date = "05.09.2026"
```

И срок доставки:

```python
delivery_days = 14
```

Используя `datetime`:

1. преобразуйте строку в дату;
2. прибавьте количество дней;
3. выведите дату доставки в формате `дд.мм.гггг`.

### Результат

```text
19.09.2026
```

---

## Задание 3. Розыгрыш призов

Дан список участников:

```python
participants = [
    "Анна",
    "Иван",
    "Мария",
    "Олег",
    "Сергей",
    "Елена",
    "Павел",
    "Ирина"
]
```

С помощью `random` случайным образом выберите трёх разных победителей.

Один человек не должен выиграть дважды.

### Пример результата

```text
Победители:
Мария
Павел
Анна
```

> Результат при каждом запуске может отличаться.

---

## Задание 4. Поиск кодов заказов

Дан текст:

```python
text = """
Заказы AB-1234 и ZX-9087 отправлены.
Код A-1234 ошибочный, а RT-0001 ожидает обработки.
"""
```

Корректный код заказа состоит из:

* двух заглавных английских букв;
* дефиса;
* четырёх цифр.

С помощью `re.findall()` найдите все корректные коды.

### Результат

```text
['AB-1234', 'ZX-9087', 'RT-0001']
```

---

## Задание 5. Расчёт времени поездки

Создайте программу:

```text
trip.py
```

Она принимает через `argparse`:

* расстояние в километрах — обязательный аргумент;
* `--speed` — необязательную скорость, по умолчанию `60`.

Считайте, что расстояние и скорость передаются положительными числами.

### Пример запуска

```text
python trip.py 420 --speed 90
```

### Результат

```text
Время в пути: 4.67 ч
```

Если скорость не указана:

```text
python trip.py 120
```

используется скорость `60 км/ч`.

### Результат

```text
Время в пути: 2.0 ч
```

---

## Задание 6. Даты внутри текста

Дан текст:

```python
text = """
Встреча состоится 12.09.2026.
Отчёт необходимо отправить 01.10.2026.
Релиз запланирован на 05.11.2026.
"""
```

С помощью регулярного выражения найдите все даты.

После этого преобразуйте каждую найденную дату с помощью `datetime.strptime()` и выведите в формате:

```text
гггг-мм-дд
```

Считайте, что все найденные даты являются корректными календарными датами.

### Результат

```text
2026-09-12
2026-10-01
2026-11-05
```

---

# Задания со звёздочкой ⭐

## Задание 7 ⭐. Приближённое вычисление числа π

Используя `random` и `math`, приблизительно вычислите число π методом случайных точек.

Алгоритм:

1. Сгенерируйте много случайных точек `(x, y)`, где `x` и `y` находятся от `-1` до `1`.
2. Посчитайте точки, которые попали внутрь круга:

```text
x² + y² <= 1
```

3. Вычислите приближённое значение:

```text
4 * количество точек внутри круга / общее количество точек
```

Используйте, например:

```python
points = 100000
```

Выведите:

* полученное значение;
* `math.pi`;
* абсолютную ошибку.

### Пример результата

```text
Получено: 3.1452
math.pi: 3.141592653589793
Ошибка: 0.003607346410207...
```

> Результат будет немного отличаться при каждом запуске.

---

## Задание 8 ⭐. Анализ журнала событий

Дан список:

```python
logs = [
    "2026-09-10 10:15:00 | 200",
    "2026-09-10 10:18:30 | 500",
    "2026-09-10 10:20:00 | 404",
    "2026-09-10 10:32:10 | 200"
]
```

Используя `re` и `datetime`:

1. извлеките дату и время каждой записи;
2. извлеките код статуса;
3. посчитайте количество ошибок — статус `400` и выше;
4. определите время между первой и последней записью списка.

### Результат

```text
Ошибок: 2
Период: 0:17:10
```

---

## Задание 9 ⭐. Планировщик работы

Создайте программу:

```text
planner.py
```

Пример запуска:

```text
python planner.py 10.09.2026 30.09.2026 --tasks 47 --per-day 6
```

Аргументы:

* первая дата — начало работы;
* вторая дата — дедлайн;
* `--tasks` — количество задач, по умолчанию `30`;
* `--per-day` — количество задач в день, по умолчанию `5`.

Необходимо:

1. с помощью регулярного выражения проверить формат дат `дд.мм.гггг`;
2. преобразовать даты через `datetime.strptime()`;
3. определить количество доступных дней;
4. через `math.ceil()` вычислить количество дней, необходимое для выполнения задач;
5. определить, получится ли выполнить всё до дедлайна.

Количество доступных дней считайте как разницу:

```text
дедлайн - дата начала
```

Дополнительно прибавлять день начала или день дедлайна не нужно.

### Для примера выше

```text
Доступно дней: 20
Необходимо дней: 8
Успеете выполнить задачи
Запас: 12 дней
```

Если необходимых дней больше:

```text
Не успеете выполнить задачи
Не хватает дней: 3
```

---

# Проект: Телефонный справочник. Часть 6

Продолжите работу с телефонным справочником.

Структуру пакета, наследование и собственные исключения из предыдущих домашних заданий сохраняем.

Теперь добавьте использование стандартной библиотеки.

## 1. Проверка номера телефона

Используйте `re`.

Допустимы номера:

```text
+79991234567
89991234567
```

Номер должен:

* начинаться с `+7` или `8`;
* после этого содержать 10 цифр.

Создайте исключение:

```python
InvalidPhoneError
```

которое наследуется от:

```python
PhoneBookError
```

Некорректный номер сохранять нельзя.

---

## 2. Дата создания контакта

При создании `Contact` сохраняйте дату и время:

```python
created_at
```

Используйте:

```python
datetime.now()
```

При выводе контакта отображайте дату в формате:

```text
дд.мм.гггг чч:мм
```

Например:

```text
Анна | +79991234567 | 11.09.2026 14:30
```

---

## 3. Случайный контакт

Добавьте в `PhoneBook` метод:

```python
random_contact()
```

Метод должен возвращать случайный контакт с помощью `random.choice()`.

Если справочник пуст, вызывайте:

```python
ContactNotFoundError
```

---

## 4. Меню

Используйте меню:

```text
1. Добавить личный контакт
2. Добавить рабочий контакт
3. Показать все контакты
4. Найти контакт
5. Изменить контакт
6. Удалить контакт
7. Показать случайный контакт
8. Выйти
```
